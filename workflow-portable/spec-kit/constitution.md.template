# {{PROJECT_NAME}} 项目宪法

> 本文件是项目的**约束源头**。spec / plan / tasks 的每一步都必须通过 constitution check。
> 修改本文件 = 修改宪法，需要显式记录变更原因和影响范围。

**版本**：v1.0.0
**生效日期**：{{YYYY-MM-DD}}
**最后修订**：{{YYYY-MM-DD}}

---

## 0. 元约束（来自 CLAUDE.md）

本宪法默认继承项目 `CLAUDE.md` 中的 Φ/Ω/Σ：

- **Φ1** 输出可外部验证 → 验收标准必须可机器验证
- **Φ2** 逆向排除优先 → 先写 MUST NOT，后写目标
- **Ω5** 范围可控 → 每个 spec 必须显式 out_of_scope
- **Ω6** 可验证 → 完成声明必须附证据

宪法各条款是 Φ/Ω 在本项目的具体化。

---

## I. 核心原则（Core Principles）

> 写 3-5 条。每条都是项目级**永远成立**的设计指导原则，不是临时约定。

### I.1 {{PRINCIPLE_1_NAME}}
{{描述：这条原则是什么、为什么重要——init 不会填，需手补}}

**违反信号**：{{什么样的代码 / 设计 = 违反了这条——手补}}

### I.2 {{PRINCIPLE_2_NAME}}
{{描述：手补}}

**违反信号**：{{手补}}

### I.3 {{PRINCIPLE_3_NAME}}
{{描述：手补}}

**违反信号**：{{手补}}

---

## II. 不可妥协项（Non-Negotiables）

> 这些是**永远不允许跨越**的红线。即使老板说要做，也必须先改宪法。

- {{NON_NEGOTIABLE_1}}
- {{NON_NEGOTIABLE_2}}
- {{NON_NEGOTIABLE_3}}
- {{NN4：例如：禁止引入 GPL 协议的依赖——init 不填，手补或删除此行}}

---

## III. 技术约束（Technical Constraints）

### III.1 技术栈
- 主语言/技术栈：{{PRIMARY_STACK}}
- 数据库：{{e.g. PostgreSQL 16——手补}}
- 部署目标：{{e.g. Vercel / 自托管 K8s——手补}}

### III.2 依赖政策
- {{e.g. 引入新依赖必须列出：替代方案、维护活跃度、license、bundle 影响}}
- {{e.g. 优先用 stdlib / 已有依赖；新增依赖需要 plan.md 显式声明}}
- {{e.g. 锁定 major 版本，禁止 ^ 范围}}

### III.3 数据契约
- {{e.g. 所有 API 响应使用统一 envelope 格式}}
- {{e.g. 所有数据库迁移必须可回滚}}
- {{e.g. 所有 schema 变更必须先写迁移测试}}

---

## IV. 质量门槛（Quality Gates）

> 任务进入 DELIVER 状态前必须满足。

### IV.1 测试
- 单元测试覆盖率 ≥ {{TEST_COVERAGE}}%
- 关键路径必须有集成测试
- 新功能必须先写测试再写实现（TDD）
- 测试不允许 mock 数据库（用 testcontainers / 真实 fixture）

### IV.2 代码
- {{e.g. 所有公共 API 必须有 TypeScript 类型}}
- {{e.g. 函数 ≤ 50 行；文件 ≤ 800 行}}
- {{e.g. 嵌套深度 ≤ 4 层}}
- {{e.g. 禁止 `any`；必须时用 `unknown` + 类型守卫}}

### IV.3 安全
- 任何用户输入必须验证（boundary validation）
- 任何对外接口必须有速率限制
- 任何错误日志不能泄漏 secret / PII
- 提交前必须 secret scan

### IV.4 性能
- {{e.g. P95 响应时间 < 300ms}}
- {{e.g. 首屏 LCP < 2.5s}}
- {{e.g. 任何 N+1 查询必须修}}

---

## V. 治理（Governance）

### 修宪流程
1. 提议者写 RFC（说明：要改哪条、为什么、影响范围、迁移路径）
2. 在 `docs/rfcs/` 下落档
3. 至少 24h 公示期（个人项目可缩短）
4. 通过后修改本文件，更新版本号 + 修订日期
5. 在 `CHANGELOG-CONSTITUTION.md` 记录变更

### Constitution Check 触发点
每个 feature 的 `plan.md` 必须有显式 constitution check 章节，逐条核对：
- [ ] 不违反任一核心原则（I.1 / I.2 / I.3 ...）
- [ ] 不踩任一不可妥协项（II.1 / II.2 ...）
- [ ] 符合所有技术约束（III）
- [ ] 设计上能满足质量门槛（IV）

任一未通过 → plan.md 打回，要么改设计，要么走修宪流程。

### 例外（Exception）
紧急情况可临时违反宪法，但必须：
- 在代码 / PR 描述中显式标记 `# CONSTITUTION-EXCEPTION: <条款编号> - <原因> - <截止偿还日期>`
- 在 `findings.md` 记录技术债
- 在偿还日期前必须修复或正式修宪

**没有标记的违反 = 漂移，必须打回。**

---

## 附录：填写检查表（接入时）

填完本文件前，确认：

- [ ] 项目名 / 版本 / 日期已填
- [ ] 至少 3 条核心原则
- [ ] 至少 3 条不可妥协项（写成 MUST NOT 形式）
- [ ] 技术栈具体到大版本号
- [ ] 质量门槛是可机器验证的（覆盖率、性能数字、行数）
- [ ] 修宪流程的执行人 / 公示位置已定
- [ ] 把所有 `{{...}}` 占位符删干净

任一不满足 → 宪法未生效，禁止进入第一个 spec。
