# Spec: {{Feature Name}}

> **SDD 第 1 步**：写需求规约（what & why），**禁止写 how**。
> 出现"用 React"、"建一张表"、"调用 X API" 之类内容 → 错位，搬到 plan.md。

**Feature ID**：{{0001}}
**作者**：{{name}}
**创建日期**：{{YYYY-MM-DD}}
**状态**：草稿 / 评审中 / 已通过 / 已废弃

---

## 0. 一句话摘要

{{用一句话说清这个 feature 解决什么。如果说不清，说明需求还没收敛。}}

---

## 1. 背景与动机（Why）

### 1.1 当前痛点
{{现在没有这个 feature 的时候，谁、在什么场景、遇到什么麻烦。要写具体场景，不写抽象描述。}}

### 1.2 目标价值
{{有了这个 feature 之后，对用户 / 业务 / 开发的具体收益。最好可量化。}}

### 1.3 不做的代价
{{如果这一轮不做会怎样？是「损失某机会」还是「累积某债务」还是「无所谓」？}}

---

## 2. 用户故事（User Stories）

### Story 1
作为 {{角色}}，我想要 {{能力}}，以便 {{价值}}。

### Story 2
作为 {{角色}}，我想要 {{能力}}，以便 {{价值}}。

> 写 1-3 个核心 story 即可。多了说明 feature 太大，应该拆。

---

## 3. 验收场景（Acceptance Scenarios）

> 每个 story 至少对应 1 个场景。用 Given/When/Then 格式。
> 这部分会被翻译成 plan.md 的契约测试和 tasks.md 的验收标准。

### Scenario 1: {{场景名}}
- **Given** {{前置状态}}
- **When** {{用户动作 / 系统事件}}
- **Then** {{期望的可观测结果}}

### Scenario 2: {{场景名}}
- **Given** ...
- **When** ...
- **Then** ...

### Edge Cases
- {{边界 1：例如：用户在断网状态下提交}}
- {{边界 2：例如：并发提交两次同一请求}}
- {{边界 3：例如：上游服务超时}}

---

## 4. 功能需求（Functional Requirements）

> 写成 "系统必须能 X" 的形式。每条都应该可独立验证。

- **FR1**：系统必须能 {{...}}
- **FR2**：系统必须能 {{...}}
- **FR3**：系统必须能 {{...}}

---

## 5. 非功能需求（Non-Functional Requirements）

> 性能 / 安全 / 可用性 / 兼容性 / 可观测性等。每条都应该可量化。

- **NFR1**：{{e.g. 接口 P95 延迟 < 200ms}}
- **NFR2**：{{e.g. 必须支持并发 1000 QPS}}
- **NFR3**：{{e.g. 错误日志必须包含 trace_id}}
- **NFR4**：{{e.g. 兼容 iOS 16+ / Android 10+}}

---

## 6. 范围边界（Scope）

### 6.1 在范围内（In Scope）
- {{...}}
- {{...}}

### 6.2 不在范围内（Out of Scope）
> 这部分必填，且必须显式。"没说要做" ≠ "不做"。

- {{e.g. 不做多语言；只支持中英}}
- {{e.g. 不做管理后台；只做面向终端用户}}
- {{e.g. 不做支付；本期只做下单流程}}

### 6.3 后续可能扩展（Future）
- {{先记下，本期不做}}

---

## 7. 依赖与影响

### 7.1 依赖什么
- {{e.g. 需要先有用户认证模块}}
- {{e.g. 依赖第三方 X 服务}}

### 7.2 影响什么
- {{e.g. 会改动现有 X 模块的 API 契约}}
- {{e.g. 会引入新的数据表 X}}

---

## 8. 开放问题（Open Questions）

> 还没想清楚的问题。本文件状态从「草稿」转「评审中」前必须清空。

- [ ] {{问题 1}}
- [ ] {{问题 2}}

---

## 9. 评审检查表

进入下一步（plan.md）前确认：

- [ ] 所有 `{{...}}` 占位符已替换
- [ ] 至少 1 个用户故事 + 1 个验收场景
- [ ] 验收场景都用 Given/When/Then 格式
- [ ] 每条 FR/NFR 可独立验证
- [ ] Out of Scope 显式列出
- [ ] Open Questions 已清空
- [ ] 没有写到 how（技术选型、架构、API 设计）

任一不满足 → 不要进入 plan 阶段。
