# Tasks: {{Feature Name}}

> **SDD 第 3 步**：把 plan.md 拆成可独立执行的 task 列表。
> 每个 task：单一职责、可独立验证、有明确依赖关系。

**Feature ID**：{{0001}}
**对应 Spec**：`./spec.md`
**对应 Plan**：`./plan.md`
**创建日期**：{{YYYY-MM-DD}}

---

## 0. 状态总览

- 总任务数：{{N}}
- 已完成：{{X}}
- 进行中：{{Y}}
- 阻塞：{{Z}}
- 当前 Phase：{{1/2/3/4/5}}

---

## 1. 任务编号规则

- 格式：`T<phase><seq>`，例如 `T101`、`T210`
- `[P]` 标记 = 可与同 phase 其他 [P] 任务**并行**执行
- 任务必须按 phase 顺序进入，不允许跳 phase
- 单个 task 完成后立即勾选 `[x]`

---

## 2. Phase 1：Setup（环境与脚手架）

> 准备工作。无业务逻辑。

- [ ] **T101** [P] {{e.g. 创建 feature 目录结构}}
  - 验收：{{命令 / 文件存在}}
- [ ] **T102** [P] {{e.g. 安装新依赖 X}}
  - 验收：{{`pnpm install` 成功 + lockfile 已更新}}
- [ ] **T103** {{e.g. 配置环境变量样板}}
  - 验收：`.env.example` 包含 {{KEY1}} {{KEY2}}
  - 依赖：T101

---

## 3. Phase 2：Tests First（TDD 红灯阶段）

> **禁止跳过。** 没有失败的测试，不允许写实现。
> 这一 phase 完成后，所有测试都应该是红的（合理失败）。

### 契约测试（对应 spec.md 的 Acceptance Scenarios）

- [ ] **T201** [P] Contract test: Scenario 1
  - 文件：`tests/contracts/{{feature}}-scenario1.test.ts`
  - 验收：测试存在、运行、失败原因 = "未实现"
- [ ] **T202** [P] Contract test: Scenario 2
  - 文件：`tests/contracts/{{feature}}-scenario2.test.ts`

### 单元测试

- [ ] **T210** [P] Unit test: {{核心函数 X}}
- [ ] **T211** [P] Unit test: {{核心函数 Y}}

### 集成测试

- [ ] **T220** Integration test: {{关键路径}}
  - 文件：`tests/integration/{{feature}}.test.ts`
  - 验收：测试覆盖 {{spec 中的 Story 1 完整路径}}

---

## 4. Phase 3：Core Implementation（绿灯阶段）

> 让 Phase 2 的测试变绿。一次只让一个测试通过，单变量迭代。

### 数据层
- [ ] **T301** {{e.g. 数据库迁移：新增 X 表}}
  - 验收：`pnpm migrate up` 成功 + `pnpm migrate down` 可回滚
  - 依赖：T103
- [ ] **T302** {{e.g. ORM 模型 X}}
  - 验收：T210 通过

### 业务逻辑
- [ ] **T310** {{e.g. 实现核心函数 X}}
  - 验收：T210 通过
  - 依赖：T302
- [ ] **T311** {{e.g. 实现核心函数 Y}}
  - 验收：T211 通过

### 接口层
- [ ] **T320** {{e.g. 实现 API 端点 POST /xxx}}
  - 验收：T201 通过
  - 依赖：T310

---

## 5. Phase 4：Integration（集成与边缘情况）

- [ ] **T401** {{e.g. 接入认证中间件}}
  - 验收：未授权返回 401
- [ ] **T402** {{e.g. 边界处理：上游服务超时}}
  - 验收：超时 5s 后返回降级响应
- [ ] **T403** {{e.g. 集成测试通过}}
  - 验收：T220 通过

---

## 6. Phase 5：Polish（打磨与上线）

- [ ] **T501** [P] {{e.g. 可观测性：加 trace_id 到日志}}
  - 验收：日志中可见 {{字段}}
- [ ] **T502** [P] {{e.g. 文档：更新 API 文档}}
- [ ] **T503** [P] {{e.g. 性能：压测 + 调优}}
  - 验收：P95 < {{NFR 中的目标}}
- [ ] **T510** {{e.g. 灰度发布：feature flag 默认关闭}}
  - 验收：flag 关闭时旧路径生效
- [ ] **T520** {{e.g. 上线观察}}
  - 验收：上线后 {{N}} 小时内关键指标无回归

---

## 7. 依赖关系图

```
T101 ──┐
T102 ──┼──→ T103 ──→ T301 ──→ T302 ──→ T310 ──→ T320 ──→ T401 ──→ T403 ──→ T510 → T520
T103 ──┘                                                                ↑
                                                                T402 ───┘
T201, T202, T210, T211 (并行) ──→ 等待对应实现 task
T220 ──→ 等待 T401
T501, T502, T503 可与 T510 并行
```

---

## 8. 任务模板（新增任务用这个）

```md
- [ ] **T<phase><seq>** {{[P] 可选}} {{任务标题}}
  - 文件：{{涉及的文件}}
  - 验收：{{机器可验证的完成条件}}
  - 依赖：{{前置任务 ID 列表，或"无"}}
  - 备注：{{可选}}
```

---

## 9. 阻塞登记

> 任何 task 卡住进入 BLOCK 时记在这里。解除后划掉。

- {{T<id>：阻塞原因 + 最小下一步}}

---

## 10. 完成判断

整个 feature 进入 DELIVER 状态需要满足：

- [ ] 所有 Phase 1-4 task 已完成
- [ ] Phase 5 关键 task 已完成（T501/T510/T520 至少完成）
- [ ] 所有契约测试通过（T2xx 系列）
- [ ] 集成测试通过（T220）
- [ ] 性能指标达成（T503）
- [ ] 灰度策略已就位（T510）
- [ ] 上线观察期通过（T520）

任一未满足 → 状态留在 WIP，不申报完成。
